export type HeaderType = {
    id: number,
    headerName: string,
    menuList: string[],
    image: string,
}

export type FooterType = {
    id: number,
    footerName: string,
    footerList: string[],
}